version: "3.8"

services:
  mongo:
    image: mongo:7
    restart: unless-stopped
    ports:
      - "27017:27017"
    volumes:
      - mongo-data:/data/db

  redis:
    image: redis:7-alpine
    restart: unless-stopped
    ports:
      - "6379:6379"
    command: ["redis-server", "--save", "20", "1", "--loglevel", "warning"]
    volumes:
      - redis-data:/data

  # feature:kafka:start
  kafka:
    image: bitnami/kafka:3.7
    restart: unless-stopped
    ports:
      - "9092:9092"
    environment:
      KAFKA_CFG_NODE_ID: 0
      KAFKA_CFG_PROCESS_ROLES: controller,broker
      KAFKA_CFG_LISTENERS: PLAINTEXT://:9092,CONTROLLER://:9093
      KAFKA_CFG_ADVERTISED_LISTENERS: PLAINTEXT://localhost:9092
      KAFKA_CFG_CONTROLLER_QUORUM_VOTERS: 0@kafka:9093
      KAFKA_CFG_CONTROLLER_LISTENER_NAMES: CONTROLLER
      KAFKA_CFG_LISTENER_SECURITY_PROTOCOL_MAP: CONTROLLER:PLAINTEXT,PLAINTEXT:PLAINTEXT
      ALLOW_PLAINTEXT_LISTENER: "yes"
    volumes:
      - kafka-data:/bitnami/kafka
  # feature:kafka:end

  app:
    build:
      context: .
      args:
        TARGET: server
        VERSION: dev
        COMMIT: docker-compose
    depends_on:
      - mongo
      # feature:kafka:start
      - kafka
      # feature:kafka:end
    ports:
      - "8002:8002"
    environment:
      APP_MONGO__URI: mongodb://mongo:27017
      # feature:kafka:start
      APP_KAFKA__BROKERS: kafka:9092
      # feature:kafka:end
      # feature:observability:start
      APP_OBSERVABILITY__ENABLED: "false"
      # feature:observability:end
    healthcheck:
      test: ["CMD", "/app/app", "healthcheck"]
      interval: 10s
      timeout: 3s
      retries: 3

volumes:
  mongo-data:
  redis-data:
  # feature:kafka:start
  kafka-data:
  # feature:kafka:end
